using UnityEngine;

public class Room : MonoBehaviour
{
    public int roomIndex; // Unique identifier for each room
    public bool isOccupied = false; // To track whether an item has spawned here
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
